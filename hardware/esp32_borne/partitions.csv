# Table de partitions (8 Mo de flash) — voir hardware/README.md §
# "Reconnaissance faciale embarquée". Une seule partition "factory", large
# (4 Mo) : le binaire embarque désormais WiFi+BLE+SD_MMC+caméra+esp-dl (ESP-WHO)
# et dépasse largement la table par défaut d'ESP-IDF (~1 Mo) — pas d'OTA
# nécessaire ici (mise à jour par flash filaire uniquement), donc une seule
# partition app plutôt qu'un schéma "two_ota".
# Name,     Type, SubType, Offset,   Size
nvs,        data, nvs,     0x9000,   0x6000
phy_init,   data, phy,     0xf000,   0x1000
factory,    app,  factory, 0x10000,  4M
